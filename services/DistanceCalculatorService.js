'use strict';

//var Promise = require('promiselib');

var DistanceCalculatorService = {

  /**
   * Calculate the distance between the users current location and the entered
   * point of interest.
   *
   * @param location
   * @returns {Promise}
   */
  calculateDistance: function(location) {
    return new Promise(function(resolve, reject){

    });
  }
};

module.exports = DistanceCalculatorService;